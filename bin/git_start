#!/bin/bash

if [ $# -ne 2 ]; then
    echo "usage: $0 NEW_BRANCH_NAME"
    exit 127
fi
branch_name=$1
git checkout master
git pull origin master
git branch $branch_name
git checkout $branch_name
git pull origin master
