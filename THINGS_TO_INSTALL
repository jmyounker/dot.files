When I install onto a new machine I should install:

sudo easy_install pip
sudo pip install virtualenv
sudo pip install virtualenvwrapper

git
brew (OSX)
brew install tig
brew install tmux
brew install reattach-to-user-namespace


